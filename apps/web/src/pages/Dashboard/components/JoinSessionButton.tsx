import React from 'react';

const JoinSessionButton: React.FC = () => {
  return (
    <button className="join-session-button">
      Join Live Session
    </button>
  );
};

export default JoinSessionButton;